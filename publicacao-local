✅ Publicar o JAR no repositório Maven local

./gradlew publishToMavenLocal

🔎 Conferir se publicou mesmo
ls ~/.m2/repository/megalodonte/simple-clipboard/1.0.0


🧪 Como usar em outro projeto (exemplo)
repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    implementation("megalodonte:simple-clipboard:1.0.0")
}